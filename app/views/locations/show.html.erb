<p id="notice"><%= notice %></p>

<div id="map">
  <img src="" id="map-image" />
<div>

<div  id="location"
      data-latitude="<%= @location.latitude %>"
      data-longitude="<%= @location.longitude %>"
>
  <p>
    <strong>Address:</strong>
    <%= @location.address %>
  </p>
</div>

<%= render partial: "call_info" %>
